{
    "Grid Movements": {
        "prefix": "grid",
        "body": [
            "const int dr[] = {1,-1,0,0,1,-1,-1,1};",
            "const int dc[] = {0,0,1,-1,1,1,-1,-1};",
            "",
            "bool ok(int x, int y, int n, int m) {",
            "    return x >= 0 && x < n && y >= 0 && y < m;",
            "}"
        ],
        "description": "Snippet para movimientos en una grid con chequeo de límites"
    }
}
