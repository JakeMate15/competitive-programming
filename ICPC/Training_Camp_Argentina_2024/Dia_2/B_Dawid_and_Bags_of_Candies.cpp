#include<bits/stdc++.h>
using namespace std;

#define all(a)  a.begin(), a.end()

void sol () {
    vector<int> a(4);
    for (auto &x: a) {
        cin >> x;
    }

    bool ok = false;


    cout << (ok ? "YES" : "NO") << "\n";
}

int main () {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t = 1;
    // cin >> t;

    while (t--) {
        sol();
    }

    return 0;
}