#include<bits/stdc++.h>
using namespace std;

#define sz(a)   a.size()

int main () {
    int t = 1;
    // cin >> t;

    while (t--) {
        int a, b;
        cin >> a >> b;

        int aux = a * b;

        cout << aux / 2 << "\n";
    }
}